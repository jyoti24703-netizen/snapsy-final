<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Snapsy – Login</title>

  <style>
    :root {
      --pink: #ff7a95;
      --yellow: #ffd166;
      --blue: #7ae7ff;
      --cyan: #a6e3e9;
    }

    body {
      margin: 0;
      padding: 0;
      background: #0f1720;               /* PURE DARK BACKGROUND */
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Poppins", sans-serif;
      color: white;
    }

    /* Card = SAME THEME AS ABOUT.CSS */
    .auth-card {
      width: 370px;
      padding: 35px;
      border-radius: 18px;
      text-align: center;
      background: linear-gradient(135deg,
        var(--pink),
        var(--yellow),
        var(--blue),
        var(--cyan)
      );
      box-shadow: 0 10px 40px rgba(0,0,0,0.5);
    }

    /* Snapsy logo in BLACK */
    .snapsy-logo {
      font-size: 46px;
      font-weight: 900;
      color: #000;                      /* BLACK LOGO */
      margin-bottom: 5px;
      letter-spacing: 1px;
      opacity: 0.9;
    }

    /* Inputs */
    .detail {
      width: 100%;
      padding: 12px;
      margin-top: 12px;
      border-radius: 10px;
      border: none;
      font-size: 15px;
      background: rgba(255,255,255,0.9);
      color: #000;
    }

    /* Button using your gradient */
    .btn-snapsy {
      width: 100%;
      padding: 12px;
      margin-top: 18px;
      font-size: 16px;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      font-weight: 600;
      background: linear-gradient(90deg, var(--pink), var(--blue));
      color: #000;
    }

    .btn-snapsy:hover {
      opacity: 0.85;
      transform: translateY(-2px);
    }

    footer {
      margin-top: 20px;
      font-size: 13px;
      color: #111;
    }

    footer a {
      color: #000;
      font-weight: 600;
      text-decoration: none;
    }

    /* Back Button */
    .back-btn {
      position: fixed;
      top: 15px;
      left: 15px;
      padding: 6px 14px;
      border-radius: 20px;
      font-size: 14px;
      background: rgba(255,255,255,0.15);
      color: white;
      text-decoration: none;
      border: 1px solid rgba(255,255,255,0.2);
    }

    .back-btn:hover {
      background: rgba(255,255,255,0.25);
    }

    .error {
      background: rgba(0,0,0,0.4);
      padding: 8px;
      border-radius: 8px;
      color: white;
      margin-bottom: 10px;
    }
  </style>
</head>

<body>

  <!-- 🔙 Back Button -->
  <a href="/" class="back-btn">← Back</a>

  <div class="auth-card">
    
    <!-- BLACK SNAPSY LOGO -->
    <div class="snapsy-logo">Snapsy</div>

    <p style="font-size:15px; margin-bottom: 12px; color:#000;">
      Login to explore beautiful snaps ✨
    </p>

    <% if (typeof error !== 'undefined' && error.length > 0) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="/login" method="post">
      <input type="text" name="username" placeholder="Username" class="detail" />
      <input type="password" name="password" placeholder="Password" class="detail" />
      <button type="submit" class="btn-snapsy">Login</button>
    </form>

    <a href="/forgot" style="font-size:13px; color:#000;">Forgot your password?</a>

    <footer>
      <p>
        By continuing, you agree to Snapsy’s
        <b>Terms of Service</b> & <b>Privacy Policy</b>.
      </p>
      <hr />
      <a href="/">Not on Snapsy yet? Sign Up</a>
    </footer>

  </div>

</body>
</html>
